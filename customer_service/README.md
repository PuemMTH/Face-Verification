### Running the Service

- **GPU mode (default):**
  ```
  uv run main.py
  ```

- **GPU mode (explicit):**
  ```
  uv run main.py --gpu_mode
  ```

- **CPU mode:**
  ```
  uv run main.py --cpu_mode
  ```